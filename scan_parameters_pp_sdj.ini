[options]
cleanOutput = True
runMadGraph = True
runPythia = True
runDelphes = True
ncpu = 1 
ncore = 7 
[AuxPars]

#zpMasses = $loop{[500.0, 600.0, 700.0, 800.0, 900.0, 1000.0, 1100.0, 1200.0, 1300.0, 1400.0, 1500.0, 1600.0, 1700.0, 1800.0, 1900.0, 2000.0, 2100.0, 2200.0, 2300.0, 2400.0, 2500.0, 2600.0, 2700.0, 2800.0, 2900.0, 3000.0]}

#sdMasses = $loop{[200, 300.0, 400, 500.0, 600.0, 700.0, 800.0, 900.0]}

#gqs = $loop{[0.01, 0.1, 0.2, 0.25]}


[MadGraphPars]
#MG5 Cards:
proccard = Cards/sd_prod2/proc_card.dat # Optional (only used if processFolder does not exist)
#proccard = Cards/sd_prod/proc_card.dat
paramcard = Cards/sd_prod2/param_card.dat # Path to the default parameter_card.dat to be used by MadGraph. Note that the parameter values defined in the MadGraphSet section below will automatically overwrite the values in paramcard. If not defined, the default file for the model will be used.
#paramcard = Cards/sd_prod/param_card.dat
runcard = Cards/sd_prod2/run_card.dat # Path to the default run_card.dat to be used by MadGraph. Note that the parameter values defined in the MadGraphSet section below will automatically overwrite the values in runcard. If not defined, the default file for the model will be used.
#runcard = Cards/sd_prod/run_card.dat
delphescard = Cards/delphes_card_trimmed.dat # If not defined, will not run Delphes
pythia8card = Cards/pythia8_card_match.dat # If not defined it will not run Pythia and Delphes
processFolder = 2mdm_sd_prod2 # Path to the folder to store the process generation output. If it already exists, the process generation will be #List of model parameters. Only values in this section are allowed as lists (for loops)
#processFolder = 2mdm_sd_prod
[MadGraphSet]

nevents = 2000
mchi = 100.0
wh = 4.070000e-03
wsd = auto 
#msd = ${AuxPars:sdMasses}
msd = 500.0
mzp = 1000.0
#mzp = ${AuxPars:zpMasses}
sa = 0.25
gqv = 0.1
gchi = 1.0
ychi = 1.0
se = 0.0
xqcut = 33.0
#run_tag = "zp_%1.0f" %(${mzp})
run_tag = "msd_%1.0f" %(${msd})
