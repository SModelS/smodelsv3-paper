[options]
cleanOutput = True
runMadGraph = True
runPythia = False
runDelphes = False
# ncpu = 1 # info not used right now
# ncore = 7 # info not used right now
[AuxPars]

zpMasses = $loop{[500.0, 700.0, 900.0, 1100.0, 1300.0, 1500.0,  1700.0, 1900.0,  2100.0, 2300.0, 2500.0, 2700.0, 2900.0, 3000.0]}

sdMasses = $loop{[250, 350.0, 450, 550.0, 650.0, 750.0, 850.0, 950.0, 1050, 1150, 1250, 1350]}

gqvs = $loop{[0.01, 0.05, 0.1, 0.2, 0.25]}

gchis = $loop{[0.1, 0.25, 0.5, 0.75, 1.0]}

[MadGraphPars]
#MG5 Cards:
#proccard = Cards/zp_prod/proc_card.dat # Optional (only used if processFolder does not exist)
proccard = Cards/zp_prod_2/proc_card.dat
#paramcard = Cards/zp_prod/param_card.dat # Path to the default parameter_card.dat to be used by MadGraph. Note that the parameter values defined in the MadGraphSet section below will automatically overwrite the values in paramcard. If not defined, the default file for the model will be used.
paramcard = Cards/zp_prod_2/param_card.dat
#runcard = Cards/zp_prod/run_card.dat # Path to the default run_card.dat to be used by MadGraph. Note that the parameter values defined in the MadGraphSet section below will automatically overwrite the values in runcard. If not defined, the default file for the model will be used.
runcard = Cards/zp_prod_2/run_card.dat
# delphescard = Cards/delphes_card_trimmed.dat # If not defined, will not run Delphes
# pythia8card = Cards/pythia8_card_match.dat # If not defined it will not run Pythia and Delphes
#processFolder = 2mdm_zp_prod # Path to the folder to store the process generation output. If it already exists, the process generation will be #List of model parameters. Only values in this section are allowed as lists (for loops)
processFolder = 2mdm_MedZp_prod
[MadGraphSet]
nevents = 10000
mchi = 65.0
wh = 4.070000e-03
wsd = auto 
msd = ${AuxPars:sdMasses}
#msd = 200.0
#mzp = 2500.0
mzp = ${AuxPars:zpMasses}
wzp = auto
sa = 0.2
gqv = ${AuxPars:gqvs}
gchi = ${AuxPars:gchis}
ychi = 1.5
se = 0.0
# xqcut = 0  
run_tag = "zp_%1.0f" %(${mzp})
#run_tag = "msd_%1.0f" %(${msd})
